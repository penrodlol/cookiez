import { CookieFormValidatorDirective } from './cookie-form-validator.directive';

describe('CookieFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CookieFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
